{
    "name": "specflux/agent-safety",
    "description": "Thin WordPress plugin host for the Agent Safety core: a governed safety/audit layer for AI agent tool calls. Wires the security package into mcp_adapter_* hooks and wp-admin, with WooCommerce supported as a capability pack.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1",
        "specflux/agent-safety-core": "@dev"
    },
    "repositories": [
        {
            "type": "path",
            "url": "../",
            "options": { "symlink": false }
        }
    ],
    "autoload": {
        "psr-4": {
            "Specflux\\AgentSafety\\Plugin\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
